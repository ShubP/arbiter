# Line-length config for pycodestyle/flake8-based IDE linters.
# The project's canonical linter/formatter is Ruff (see pyproject.toml).
[flake8]
max-line-length = 88
extend-ignore = E203, W503

[pycodestyle]
max-line-length = 88
